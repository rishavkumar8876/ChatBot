{
  "name": "apnagpt-monorepo",
  "version": "1.0.0",
  "description": "Root configuration for Render deployment",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "node backend/server.js"
  }
}
